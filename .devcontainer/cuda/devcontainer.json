{
  "name": "PyTorch Dev Container (CUDA)",
  "build": {
    "dockerfile": "../Dockerfile",
    "context": "${localWorkspaceFolder}",
    "args": {
      "PLATFORM": "cuda"
    }
  },
  "workspaceFolder": "/workspaces/diffusion",
  "remoteEnv": {
    "VIRTUAL_ENV": "/opt/venv",
    "PATH": "/opt/venv/bin:${containerEnv:PATH}"
  },
  "runArgs": [
    "--gpus=all"
  ],
  "mounts": [
    "source=/root/.gitconfig,target=/root/.gitconfig,type=bind,consistency=cached"
  ],
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance",
        "ms-azuretools.vscode-docker"
      ],
      "settings": {
        "python.defaultInterpreterPath": "/opt/venv/bin/python",
        "python.languageServer": "Pylance",
        "python.terminal.activateEnvironment": true
      }
    }
  },
  "postCreateCommand": "echo 'source /opt/venv/bin/activate' >> ~/.bashrc && show-platform",
  "remoteUser": "root"
}
